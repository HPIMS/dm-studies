{
  "private": true,
  "scripts": {
    "build": "node build",
    "sync": "node sync"
  },
  "devDependencies": {
    "ajv": "^7.0.3",
    "yaml": "^1.10.0"
  },
  "dependencies": {
    "node-fetch": "^2.6.1",
    "querystring": "^0.2.1"
  }
}
