{
  "private": true,
  "scripts": {
    "build": "node scripts/build && node scripts/build_v2 && node scripts/build_v3",
    "diff": "node scripts/diff"
  },
  "devDependencies": {
    "ajv": "^7.0.3",
    "chalk": "^4.1.1",
    "yaml": "^2.0.0-9"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
